<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>non-user-query.xml file</comment>
	
	<!-- 아이디 중복 검사 -->
	<entry key="idDupCheck">
		SELECT COUNT(*) FROM "USER"
		WHERE USER_ID = ?
		AND U_SECESSION_FG = 'N'
	</entry>
	
	<!-- 닉네임 중복 검사 -->
	<entry key="nicknameDupCheck">
		SELECT COUNT(*) FROM "USER"
		WHERE NICKNAME = ?
		AND U_SECESSION_FG = 'N'
	</entry>
	
	<!-- 회원가입 -->
	<entry key="signUp">
		INSERT INTO "USER" 
		VALUES( SEQ_USER_NO.NEXTVAL, ?, ?, ?, ?,
		?, ?, NULL, DEFAULT, DEFAULT)
	</entry>
	
	<entry key="login">
		SELECT USER_NO, USER_ID, USER_NAME, NICKNAME, EMAIL, PHONE, PROFILE,
			TO_CHAR(ENROLL_DATE, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') ENROLL_DATE 
		FROM "USER"
		WHERE USER_ID = ?
		AND USER_PW = ?
		AND U_SECESSION_FG = 'N'
	</entry>
</properties>
